# Battle for Mount Hyjal

- Map ID: `534`
- Status: scaffold only
- Planned phase: Later phase
- WLK source mapping: `src/Ai/Raid/Hyjal`

No Strategy, Action, Trigger or Multiplier is registered from this
directory yet.

Expected initial files:

```text
BattleforMountHyjalStrategy.h/.cpp
BattleforMountHyjalActions.h/.cpp
BattleforMountHyjalTriggers.h/.cpp
BattleforMountHyjalMultipliers.h/.cpp
BattleforMountHyjalHelpers.h/.cpp
```

Ported code must use the interfaces in `../common/` and record its
upstream source commit.
