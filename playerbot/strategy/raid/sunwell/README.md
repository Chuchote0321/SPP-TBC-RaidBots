# Sunwell Plateau

- Map ID: `580`
- Status: scaffold only
- Planned phase: Separate development scope
- WLK source mapping: `No complete source assumed`

No Strategy, Action, Trigger or Multiplier is registered from this
directory yet.

Expected initial files:

```text
SunwellPlateauStrategy.h/.cpp
SunwellPlateauActions.h/.cpp
SunwellPlateauTriggers.h/.cpp
SunwellPlateauMultipliers.h/.cpp
SunwellPlateauHelpers.h/.cpp
```

Ported code must use the interfaces in `../common/` and record its
upstream source commit.
