# Black Temple

- Map ID: `564`
- Status: scaffold only
- Planned phase: Later phase
- WLK source mapping: `src/Ai/Raid/BlackTemple`

No Strategy, Action, Trigger or Multiplier is registered from this
directory yet.

Expected initial files:

```text
BlackTempleStrategy.h/.cpp
BlackTempleActions.h/.cpp
BlackTempleTriggers.h/.cpp
BlackTempleMultipliers.h/.cpp
BlackTempleHelpers.h/.cpp
```

Ported code must use the interfaces in `../common/` and record its
upstream source commit.
