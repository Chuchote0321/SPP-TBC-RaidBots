# Serpentshrine Cavern

- Map ID: `548`
- Status: scaffold only
- Planned phase: Later phase
- WLK source mapping: `src/Ai/Raid/SerpentshrineCavern`

No Strategy, Action, Trigger or Multiplier is registered from this
directory yet.

Expected initial files:

```text
SerpentshrineCavernStrategy.h/.cpp
SerpentshrineCavernActions.h/.cpp
SerpentshrineCavernTriggers.h/.cpp
SerpentshrineCavernMultipliers.h/.cpp
SerpentshrineCavernHelpers.h/.cpp
```

Ported code must use the interfaces in `../common/` and record its
upstream source commit.
