# Gruul's Lair

- Map ID: `565`
- Status: scaffold only
- Planned phase: First vertical slice
- WLK source mapping: `src/Ai/Raid/Gruul`

No Strategy, Action, Trigger or Multiplier is registered from this
directory yet.

Expected initial files:

```text
GruulsLairStrategy.h/.cpp
GruulsLairActions.h/.cpp
GruulsLairTriggers.h/.cpp
GruulsLairMultipliers.h/.cpp
GruulsLairHelpers.h/.cpp
```

Ported code must use the interfaces in `../common/` and record its
upstream source commit.
