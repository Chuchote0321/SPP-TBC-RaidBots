# WCL position data

`wcl_position_samples.csv` is intentionally empty until actual WCL positional events are retrieved. Do not hand-enter guessed coordinates.

Accepted rows should preserve raw report-space x/y plus report/fight/time metadata. Normalization and aggregation belong in analysis tooling; compiled encounter code consumes only the derived canonical profile.
