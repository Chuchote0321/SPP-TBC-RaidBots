# Tempest Keep: The Eye

- Map ID: `550`
- Status: scaffold only
- Planned phase: Later phase
- WLK source mapping: `src/Ai/Raid/TempestKeep`

No Strategy, Action, Trigger or Multiplier is registered from this
directory yet.

Expected initial files:

```text
TempestKeepTheEyeStrategy.h/.cpp
TempestKeepTheEyeActions.h/.cpp
TempestKeepTheEyeTriggers.h/.cpp
TempestKeepTheEyeMultipliers.h/.cpp
TempestKeepTheEyeHelpers.h/.cpp
```

Ported code must use the interfaces in `../common/` and record its
upstream source commit.
